---
label: Podcast Post
hide_body: false
fields:
- name: layout
  type: text
  config:
    required: false
  label: layout
  description: Used to determine how to display the post. Do not change.
  hidden: true
  default: podcast-post
- name: categories
  type: text
  config:
    required: false
  label: categories
  description: Used to determine how to display the post. Do not modify.
  default: agile-in-action-poscast
  hidden: true
- name: date
  type: datetime
  label: Publish date
  description: Select the day the podcast goes live
  config:
    required: false
    date_format: YYYY-MM-DD
    time_format: 
    display_utc: false
- name: author
  type: select
  default: Bill Raymond
  config:
    required: false
    options:
    - Bill Raymond
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Podcast host
  hidden: true
  description: Select the name of the person hosting the podcast. The person will
    also be listed as the author in the website.
- name: title
  type: text
  config:
    required: true
  label: Podcast title
  description: A short title to describe the podcast
- name: guestDetails
  type: field_group_list
  fields:
  - name: name_of_person
    type: text
    config:
      required: false
    label: Guest name
    description: 'Type the guest''s name, or name and title. Like this: "Sarah Smith"
      or "Sarah Smith, Vice President Technology at Acme Corp".'
  - name: guest_photo
    type: file
    config:
      maxSize: 64
    label: Guest photo
  - name: guest_links
    type: field_group_list
    fields:
    - name: guestLinkLabel
      type: text
      config:
        required: false
      label: label
      description: Type a label for the link, like LinkedIn, Twitter, Amazon Book,
        etc.
    - name: guestLinkUrl
      type: text
      config:
        required: false
      label: Link url
      description: Type a url for the link, like https://www.mybook.com.
    config:
      min: 
      max: 
      labelField: 
    label: Guest links
    description: Add links, like LinkedIn and others for each guest.
  config:
    min: 
    max: 
    labelField: 
  label: Guest details
  description: Select the guest's photo and their name. There must be at least one
    guest and not more than four guests.
- name: libsynUrl
  type: text
  config:
    required: false
  label: Libsyn directory url
  description: After you publish a post, Libsyn will provide a "Libsyn Directory URL".
    Paste that link into this field.
pages:
- _posts/2021-08-17-agile-and-drones.md
- _posts/2021-08-17-test-guest-details.md
- _posts/reama-is-testing-the-mvp.md
- _posts/the-scrum-war-of-2024.md
